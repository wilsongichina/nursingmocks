const fs = require('node:fs');
const path = require('node:path');
const out = path.join(process.cwd(), 'reports/routing-repairs-2026-09-17');
const read = name => JSON.parse(fs.readFileSync(path.join(out, name), 'utf8'));
const audit = read('summary.json');
const tests = read('unit-test-results.json');
const http = read('local-http-results.json');
const data = read('data-repair-result.json');
const esc = value => String(value ?? '').replace(/[&<>"']/g, c => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', '"':'&quot;', "'":'&#39;' }[c]));
const failures = http.filter(row => row.errors.length);
const issues = Object.values(audit.counts).reduce((sum, count) => sum + count, 0);
const sitemapCount = http.find(row => row.path === '/sitemap.xml')?.urlCount;
const changes = [
  ['ATI TEAS Review Mode', 'All 56 canonical subject/set URLs resolve to their saved quizzes. Review Mode opens the selected set at #questions-start.'],
  ['Removed /ati-teas', 'The public mapping was removed and the article was preserved as a draft with no public slug. The local application returns HTTP 404 and excludes it from public lists. /ati-teas-practice-test remains available.'],
  ['Knowledge-base hierarchy', 'Three active articles were reattached to their matching RN Exit, LPN Test Bank, and ATI TEAS parent pages. Breadcrumbs use the article’s real parent rather than its article ID. Missing parents never become raw-ID links.'],
  ['Missing quiz', 'Restored the route for Mental Health ATI RN Proctored Exam Practice Questions. Its 34 saved questions and access package were preserved.'],
  ['Navigation and redirects', 'HESI A2 and Nursing navigation now use valid destinations. Added the missing How It Works and FAQ pages. Old exit-exam URLs and legacy TEAS URLs retain query parameters while redirecting to the correct page.'],
  ['Canonical URLs', 'Dynamic pages declare their own canonical URL, including HESI quizzes without custom metadata. Knowledge-base noindex remains intact. Main exam pages and blog pages no longer inherit the homepage canonical.'],
  ['Sitemap', `Rebuilt from current route/content publication metadata: ${sitemapCount} unique URLs. Excludes draft, missing, retired, private and deliberately noindexed knowledge-base content. Does not invent modification dates.`],
  ['Missing-page handling', 'Missing blog posts and invalid knowledge-base categories return a not-found page and HTTP 404. Quiz slugs cannot masquerade as knowledge-base categories. Public article rendering no longer waits for browser-only authentication.'],
];
const status = !failures.length && !issues && tests.success && data.verified ? 'Validation passed' : 'Review outstanding failures';
const html = `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>NursingMocks routing repairs — 17 September 2026</title><style>
*{box-sizing:border-box}body{margin:0;background:#f6f7fb;color:#17223b;font:16px/1.6 system-ui,sans-serif}main{max-width:1120px;margin:auto;padding:32px 20px}h1{font-size:clamp(26px,4vw,38px);line-height:1.2}h2{margin-top:0;font-size:23px}h3{font-size:17px}a{color:#4f35b6}section{background:white;border:1px solid #dfe3ed;border-radius:14px;margin:20px 0;padding:24px}.badge{display:inline-block;background:#e6f6ec;color:#155a33;padding:5px 12px;border-radius:999px;font-weight:650}.note{background:#fff5dc;border-left:4px solid #d69417;padding:16px}.metrics{display:grid;grid-template-columns:repeat(auto-fit,minmax(170px,1fr));gap:12px}.metric{background:#f1eefc;border-radius:10px;padding:16px}.metric b{display:block;font-size:28px}.table-wrap{overflow:auto}table{border-collapse:collapse;width:100%;text-align:left;font-size:14px}th,td{padding:12px;border-bottom:1px solid #e6e9f0;vertical-align:top}th{background:#f1f3f8}.path{overflow-wrap:anywhere}.pass{color:#166534}.fail{color:#b91c1c}small{color:#526078}li{margin:7px 0}code{overflow-wrap:anywhere}footer{font-size:13px;color:#526078}
</style></head><body><main><p class="badge">${esc(status)}</p><h1>NursingMocks routing repairs</h1><p>17 September 2026 · Fixes prepared before commit and Vercel deployment.</p>
<p class="note"><strong>Deployment status:</strong> Application changes are local and uncommitted. The six targeted database corrections have already been applied and verified, with a backup. The updated Vercel application has not been deployed.</p>
<section><h2>Validation at a glance</h2><div class="metrics"><div class="metric"><b>${audit.routeMappings.toLocaleString()}</b>route mappings checked</div><div class="metric"><b>${issues}</b>mapping/breadcrumb findings</div><div class="metric"><b>${tests.numPassedTests}/${tests.numTotalTests}</b>automated tests passed</div><div class="metric"><b>${http.length-failures.length}/${http.length}</b>local HTTP checks passed</div></div><p>All ${audit.sidebarLinks} sidebar destinations and ${audit.sourceLinks} literal source links resolved in the metadata audit. TypeScript validation and optimized production compilation passed.</p></section>
<section><h2>What was repaired</h2>${changes.map(([title, detail]) => `<h3>${esc(title)}</h3><p>${esc(detail)}</p>`).join('')}</section>
<section><h2>Database changes already applied</h2><p>One transaction changed six records. Verification compared complete records with the expected result to detect unrelated changes. No question, user, purchase, entitlement or assignment records were modified.</p><ul><li>Updated three orphaned article parent references.</li><li>Preserved the /ati-teas article as a draft, cleared its public slug and removed its public route mapping.</li><li>Created the missing mental-health quiz mapping with its original hierarchy and Nursing Test Bank access package.</li></ul><p><a href="data-repair-result.json">Verified transaction result</a> · <a href="${esc(path.basename(data.backupFile))}">Original affected records</a></p></section>
<section><h2>Local production-server checks</h2><p>These checks used the compiled application on port 3011, with three concurrent requests. They checked response codes, destinations and query strings, canonical paths, quiz question payloads, breadcrumb hierarchy, and sitemap contents.</p><div class="table-wrap"><table><thead><tr><th>Path</th><th>HTTP</th><th>Result</th></tr></thead><tbody>${http.map(row => `<tr><td class="path">${esc(row.path)}</td><td>${esc(row.status)}</td><td class="${row.errors.length?'fail':'pass'}">${row.errors.length?esc(row.errors.join('; ')):'Pass'}</td></tr>`).join('')}</tbody></table></div></section>
<section><h2>Scope and practical limits</h2><ul><li>The full registry and source-link audit covered ${audit.routeMappings} mappings. The ${http.length} HTTP checks sampled each repaired behavior; they were not a second 1,940-page production crawl.</li><li>Production compilation used Next.js compile mode. It validates optimized compilation and permits runtime checks; full static generation of every page remains part of the normal deployment build.</li><li>No connected browser was available for an interactive or signed-in walkthrough. Paid-question access, checkout, admin actions and visual/responsive behavior were not manually exercised.</li><li>An intermittent authentication-context error appeared during development-server recompilation. It was not reproduced in the concurrent local production-server checks.</li><li>Metadata now resolves before response headers, allowing genuine 404 status codes. Uncached dynamic requests can wait for metadata retrieval; existing page caching remains enabled. See <a href="https://nextjs.org/docs/15/app/api-reference/config/next-config-js/htmlLimitedBots">Next.js blocking-metadata documentation</a>.</li><li>Existing production caches can retain old pages until revalidation. Local application fixes take effect publicly after deployment.</li></ul></section>
<section><h2>Evidence</h2><p><a href="summary.json">Registry audit summary</a> · <a href="findings.json">Registry findings</a> · <a href="local-http-results.json">HTTP results</a> · <a href="unit-test-results.json">Automated test results</a> · <a href="local-sitemap.xml">Generated sitemap</a> · <a href="production-compile.log">Compilation log</a> · <a href="../routing-and-breadcrumbs-2026-09-17/index.html">Original audit</a></p></section><footer>Generated ${esc(new Date().toISOString())}. Questions and saved content IDs preserved.</footer></main></body></html>`;
fs.writeFileSync(path.join(out, 'index.html'), html);
fs.writeFileSync(path.join(out, 'REPORT.md'), `# NursingMocks routing repairs — 17 September 2026\n\n${status}. Application code is local and uncommitted; no Vercel deployment. Six database repairs were applied with backup and verified.\n\n## Changes\n\n${changes.map(([title, detail]) => `- **${title}:** ${detail}`).join('\n')}\n\n## Validation\n\n- ${audit.routeMappings} route mappings, zero findings. ${audit.sourceLinks} literal source links and ${audit.sidebarLinks} sidebar links resolved.\n- ${tests.numPassedTests}/${tests.numTotalTests} automated tests passed. TypeScript passed.\n- ${http.length-failures.length}/${http.length} local production-server HTTP checks passed.\n- Optimized production compilation passed in compile mode; full static generation and signed-in browser walkthrough are not included.\n- Development-server context error was not reproduced in concurrent production-server checks.\n\nOpen index.html for the detailed report and evidence.\n`);
console.log(`Created ${path.join(out, 'index.html')}`);
